# === Server (Render) ===
PORT=5000

# Never commit real URIs or secrets here — placeholders only.
MONGODB_URI=__SET_IN_RENDER_DASHBOARD__
JWT_SECRET=__SET_IN_RENDER_DASHBOARD__
SESSION_SECRET=__SET_IN_RENDER_DASHBOARD__

# Comma-separated list of allowed frontend origins (CORS)
# Include localhost for dev + production domain + preview wildcard.
CLIENT_URL=http://localhost:5173,https://marrakechdunes.vercel.app,https://marrakechdunes-*.vercel.app

# Optional (set only if you actually use email)
EMAIL_USER=
EMAIL_PASS=

# Optional integrations
WHATSAPP_RECEIVERS=

# Console login passwords (set in Render dashboard, not here)
ADMIN_PASSWORD=__SET_IN_RENDER_DASHBOARD__
SUPERADMIN_PASSWORD=__SET_IN_RENDER_DASHBOARD__

NODE_ENV=production
LOG_LEVEL=info


# === Client (Vercel) ===
# Where the frontend calls your API (Render)
VITE_API_URL=https://marrakechdunesr.onrender.com
# Where images/static files come from
VITE_ASSETS_BASE=https://marrakechdunesr.onrender.com/attached_assets
