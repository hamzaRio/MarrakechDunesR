# Exclude sensitive files from Cursor context
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.redacted

# Exclude node_modules and build artifacts
node_modules/
dist/
build/
client/dist/
server/dist/
*.tsbuildinfo

# Exclude logs and cache
logs/
*.log
.cache/
.vite/
client/.vite/
server/.vite/

# Exclude IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Exclude OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Exclude temporary files
tmp/
temp/
uploads/

# Exclude lockfiles (too large for context)
package-lock.json
yarn.lock

# Exclude test results
test-results/
coverage/
*.lcov

# Note: .env.example is NOT excluded - it should be included in context
